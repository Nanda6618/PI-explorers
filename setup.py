from setuptools import setup, find_packages

setup(
    name='piexplorers',  # Replace 'piexplorers' with your desired package name
    version='0.1',  # Specify your package version
    packages=find_packages(),  # Automatically find packages in the project directory
    install_requires=[],  # Add any dependencies your package requires
)
